{
  "name": "electron-ssh-client",
  "version": "1.0.0",
  "description": "Electron SSH Client with multi-tab and multi-window support",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win --x64",
    "build:dir": "electron-builder --win --x64 --dir",
    "pack": "electron-builder --dir --config.compression=store",
    "renderer:dev": "node -e \"const http=require('http'),fs=require('fs'),path=require('path');const roots=[path.join(process.cwd(),'renderer'),path.join(process.cwd(),'..','renderer')];const root=roots.find(p=>fs.existsSync(path.join(p,'index.html')));const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.svg':'image/svg+xml','.ico':'image/x-icon'};if(!root)throw new Error('renderer directory not found');http.createServer((req,res)=>{let pathname=decodeURIComponent(new URL(req.url,'http://localhost').pathname);if(pathname==='/')pathname='/index.html';const file=path.normalize(path.join(root,pathname));if(!file.startsWith(root)){res.writeHead(403);res.end('Forbidden');return;}fs.readFile(file,(err,data)=>{if(err){res.writeHead(404);res.end('Not found');return;}res.writeHead(200,{'Content-Type':types[path.extname(file)]||'application/octet-stream'});res.end(data);});}).listen(1420,'127.0.0.1',()=>console.log('renderer dev server http://localhost:1420'))\"",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "build": {
    "appId": "com.sshclient.app",
    "icon": "assets/icon.ico",
    "productName": "SSH Client",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "db.js",
      "renderer/**/*",
      "assets/**/*",
      "node_modules/**/*",
      "!node_modules/electron/**/*",
      "!node_modules/electron-builder/**/*",
      "!node_modules/.cache/**/*"
    ],
    "win": {
      "target": [
      { 
        "target": "nsis", 
        "arch": ["x64"] 
      },
      { 
        "target": "portable",  
        "arch": ["x64"] 
      }
    ],
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "asar": true,
    "asarUnpack": [
      "node_modules/sql.js/**/*",
      "node_modules/ssh2/**/*",
      "node_modules/cpu-features/**/*",
      "node_modules/sshcrypto/**/*",
      "node_modules/mysql2/**/*"
    ],
    "npmRebuild": false
  },
  "dependencies": {
    "electron-store": "^8.2.0",
    "mysql2": "^3.22.0",
    "sql.js": "^1.10.3",
    "ssh2": "^1.17.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0",
    "xterm-addon-web-links": "^0.9.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.3",
    "electron": "^27.3.11",
    "electron-builder": "^26.8.1"
  },
  "author": "",
  "license": "MIT"
}
